# Local dev only: the frontend runs on Next's own dev server, separate from
# the backend on :8000. Copy to .env.local. Leave unset in production — the
# backend serves this static export same-origin there (see root Dockerfile).
NEXT_PUBLIC_API_BASE=http://localhost:8000
